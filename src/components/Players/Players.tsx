import { AddPlayer } from './AddPlayer';
import { PlayersList } from './PlayersList';

export const Players = () => {
    return (
        <>
            <AddPlayer />
            <PlayersList />
        </>
    );
};
